# TIP-035 Final Promotion

Final production identity: `0.2.34 / TIP-033`.

## Promotion basis

- TIP-034A Tunnel B autostart: PASS.
- TIP-034B cross-account/FIFO/idempotency: PASS.
- TIP-034C repository bootstrap completeness: PASS.
- TIP-034D current-VPS native qualification: PASS.
- TIP-034E formal current-build soak: PASS on `TIP-033RC1` (60 minutes, 120/120 samples, zero bad episodes, zero PID/generation changes).
- Exact-head TIP-027, TIP-028, and TIP-034 GitHub workflows: PASS before promotion.

## Identity change

`bridge_build` only: `TIP-033RC1` -> `TIP-033`.

No tool catalog, schema, MT5 account, credentials, AutoTrading, or trading/runtime behavior change is introduced by the promotion commit.

## Required post-merge acceptance

1. Deploy the promoted provenance to `C:\VibeMQL5`.
2. Restart the control plane without touching MT5 account/AutoTrading.
3. Verify `server_info`, `health`, `runtime_status`, `diagnose`, and A/B tunnel status.
4. Run short fixed-MT5-2 DemoEA native acceptance and exact replay.
5. Run formal 60-minute TIP-033 soak so `current_runtime_certification=true` binds to the final build identity.
